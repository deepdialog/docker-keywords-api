# docker-keywords-api

```bash
$ docker build -f Dockerfile -t qhduan/kw-extraction-api:0.1 .

# $ docker run -it --rm -p 8000:8000 qhduan/kw-extraction-api:0.1
$ docker run -d --name=kw-extraction -p 8000:8000 qhduan/kw-extraction-api:0.1
```